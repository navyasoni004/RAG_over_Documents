graph TB    %% ─────────── Presentation Layer ───────────    subgraph UI["🖥️  Presentation Layer · Streamlit"]        UPLOAD["📄 PDF Upload Widget"]        CHAT["💬 Chat Interface<br/>(history + streaming)"]        EMB_BTN["☐ Create Embeddings Toggle"]    end    %% ─────────── Ingestion Pipeline ───────────    subgraph INGEST["📥 Ingestion Pipeline · vectors.py"]        direction TB        LOADER["PyPDFLoader<br/>──────────<br/>extract_pages → text"]        SPLITTER["RecursiveCharacterTextSplitter<br/>──────────<br/>chunk_size=1000<br/>overlap=250<br/>separators=[\\n\\n, \\n, ' ', '']"]        EMBED["BAAI/bge-small-en<br/>──────────<br/>HuggingFace Embeddings<br/>384-dim vectors · CPU"]        STORE["Chroma Collection<br/>──────────<br/>persist_directory=./chroma_db<br/>collection=rag_docs"]        LOADER --> SPLITTER --> EMBED --> STORE    end    %% ─────────── Retrieval Pipeline ───────────    subgraph RETRIEVE["🔍 Retrieval Pipeline · chatbot.py"]        direction TB        Q_EMBED["Query Embedder<br/>──────────<br/>bge-small-en<br/>(same model)"]        SIM["Similarity Search<br/>──────────<br/>cosine distance<br/>top-k = 3"]        RANK["Context Assembler<br/>──────────<br/>merge + dedupe chunks<br/>format as context string"]        Q_EMBED --> SIM --> RANK    end    %% ─────────── Generation Layer ───────────    subgraph GEN["🧠 Generation Layer"]        PROMPT["Prompt Template<br/>──────────<br/>System: 'Answer ONLY from<br/>context. Say I don't know<br/>if not found.'"]        LLM["Qwen2.5-7B-Instruct<br/>──────────<br/>HuggingFace Inference API<br/>temp=0.3 · max_tokens=512"]        CHAIN["RetrievalQA Chain<br/>──────────<br/>LangChain orchestrator"]        PROMPT --> CHAIN        LLM --> CHAIN    end    %% ─────────── Storage / Config ───────────    subgraph STORE_LAYER["💾 Storage & Config"]        CHROMA_PERSIST[("🗄️ ChromaDB<br/>Persistent Disk Store")]        ENV["🔐 .env<br/>HUGGINGFACEHUB_API_TOKEN"]    end    %% ─────────── External ───────────    subgraph EXT["☁️ External Service"]        HF_API["HuggingFace Cloud<br/>Inference Endpoints"]    end    %% ───── Inter-layer connections ─────    UPLOAD -->|"file bytes"| LOADER    EMB_BTN -->|"trigger"| EMBED    STORE <-->|"read/write"| CHROMA_PERSIST    EMBED -.->|"uses key"| ENV    CHAT -->|"user question"| Q_EMBED    STORE -->|"similarity query"| SIM    RANK -->|"context + question"| PROMPT    CHAIN -->|"POST / inference"| HF_API    HF_API -->|"generated text"| CHAIN    CHAIN -->|"grounded answer"| CHAT    %% ───── Styling ─────    classDef uiLayer fill:#1a1a2e,stroke:#e94560,stroke-width:2px,color:#fff    classDef ingestLayer fill:#0f3460,stroke:#00d9ff,stroke-width:2px,color:#fff    classDef retrieveLayer fill:#16213e,stroke:#0f9,stroke-width:2px,color:#fff    classDef genLayer fill:#2d1b4e,stroke:#b388ff,stroke-width:2px,color:#fff    classDef storage fill:#1b1b1b,stroke:#ffd700,stroke-width:2px,color:#fff    classDef external fill:#2a0a3e,stroke:#ff6b6b,stroke-width:2px,color:#fff    class UPLOAD,CHAT,EMB_BTN uiLayer    class LOADER,SPLITTER,EMBED,STORE ingestLayer    class Q_EMBED,SIM,RANK retrieveLayer    class PROMPT,LLM,CHAIN genLayer    class CHROMA_PERSIST,ENV storage    class HF_API external